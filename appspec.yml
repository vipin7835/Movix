version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/reactapp

permissions:
  - object: /home/ubuntu/reactapp
    owner: ubuntu
    group: ubuntu
    mode: 775
    type:
      - directory
      - file
